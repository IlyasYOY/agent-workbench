interface:
  display_name: "Setup Codex"
  short_description: "Configure Codex from agent-workbench safely"
  default_prompt: "Use $setup-codex to review and configure my Codex settings."

policy:
  allow_implicit_invocation: false
