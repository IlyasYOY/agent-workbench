interface:
  display_name: "Setup Codex"
  short_description: "Reconcile Codex config and remote skills safely"
  default_prompt: "Use $setup-codex to review and reconcile my Codex config and remote-skill policy."

policy:
  allow_implicit_invocation: false
