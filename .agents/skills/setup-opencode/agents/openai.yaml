interface:
  display_name: "Setup OpenCode"
  short_description: "Configure OpenCode from agent-workbench safely"
  default_prompt: "Use $setup-opencode to review and configure my OpenCode settings."

policy:
  allow_implicit_invocation: false
